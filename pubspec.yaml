name: flutter_input_chips
description: Flutter widget that builds the input field with input chips options
version: 1.1.0
homepage: https://github.com/imsujan276/flutter_input_chips

environment:
  sdk: ">=2.18.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter: